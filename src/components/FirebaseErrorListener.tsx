'use client';

/**
 * Error listener component - Firebase removed, using MySQL database now.
 * This component is kept for compatibility but is no longer active.
 * Error handling is managed through API error responses.
 */
export function FirebaseErrorListener() {
  return null; // No-op component, Firebase removed
}
